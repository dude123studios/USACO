#include <bits/stdc++.h>
using namespace std;
int N, x = 0, y = 0, xs[40], ys[40], gx, gy;
set<set<int>> paths;


int main()
{
    cin >> N;
    cin >> gx >> gy;
    for (int i = 0; i < N; i++)
        cin >> xs[i] >> ys[i];
    }
    int ha
}