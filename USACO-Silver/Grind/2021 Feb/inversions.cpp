#include <bits/stdc++.h>
using namespace std;

int main()
{
    string l;
    int N, M;
    cin >> l;
    N = l.size();
    cin >> M;
    set<int> inversions, multiset<int> lengths;
    inversions.insert(0);
    for (int i = 0; i < N; i++)
    {
        l[i]
    }
    for (int i = 0; i < M; i++)
    {
    }
}